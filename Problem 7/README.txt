To run,
1. Ensure all files in this folder are in the same project folder when running.
2. Install dependencies with "pip install numpy", "python -m pip install -U matplotlib", and "pip install pandas"
3. run Parkinsons.py - use Python3 to run
